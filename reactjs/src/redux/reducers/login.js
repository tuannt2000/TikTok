import * as types from '../constants/login';

const initState = {
    data: {},
    loginSuccess: false
};

export const loginReducer = (state = initState, action) => {
    switch (action.type) {

        default:
            return state;
    }
};