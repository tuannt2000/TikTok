import * as types from '../constants/comment';

const initState = {
    listComment: [],
};

export const commentReducer = (state = initState, action) => {
    switch (action.type) {
        default:
            return state;
    }
};