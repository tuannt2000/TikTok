import * as types from '../constants/video';

const initState = {

};

export const videoReducer = (state = initState, action) => {
    // switch (action.type) {
    //
    //     default:
    //         return state;
    // }
};